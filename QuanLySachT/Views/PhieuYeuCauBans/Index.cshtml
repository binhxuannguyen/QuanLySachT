@model IEnumerable<QuanLySachT.Models.PhieuYeuCauBan>

@{
    ViewBag.Title = "Index";
}

<h2>Danh sách phiếu yêu cầu bán</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NgayGio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NhaXuatBan.TenNXB)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    if (item.TinhTrang == true)
    {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NgayGio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NhaXuatBan.TenNXB)
        </td>
        <td>
            @Html.ActionLink("Create","Index","CT_PhieuYeuCauBan") |
            @Html.ActionLink("Edit", "Edit", new { id = item.MaPhieu }) |
            @Html.ActionLink("Details", "Details", new { id = item.MaPhieu }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.MaPhieu })
        </td>
    </tr>
    }
    /*<tr>
        <td>
            @Html.DisplayFor(modelItem => item.NgayGio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NhaXuatBan.TenNXB)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaPhieu }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaPhieu }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaPhieu })
        </td>
    </tr>*/
}

</table>
