@model IEnumerable<QuanLySachT.Models.DaiLy>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenDaiLy)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SoNo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenDaiLy)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SoNo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaDaiLy }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaDaiLy }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaDaiLy })
        </td>
    </tr>
}

</table>
